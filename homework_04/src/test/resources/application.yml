spring:
  shell:
    interactive:
      enabled: false

application:
  fileName: "testdata"
  locale: en_US
  numberCorrectAnswers: 4

messagesource:
  basename: "/i18n/bundle"
  defaultEncoding: "UTF-8"
