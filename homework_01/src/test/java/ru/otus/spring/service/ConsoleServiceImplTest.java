package ru.otus.spring.service;

import org.junit.Test;
import org.springframework.context.MessageSource;
import ru.otus.spring.domain.Student;
import org.mockito.Mockito;
import java.util.Locale;

import static org.assertj.core.api.Assertions.assertThat;
import static org.mockito.Mockito.mock;

class ConsoleServiceImplTest {
/*

    @Test
    public void askStudentInfoTest() {
      /*  ConsoleService ioMock = mock(ConsoleService.class);
        ConsoleService console = new ConsoleServiceImpl(ioMock);
        Mockito.when(ioMock.readString()).thenReturn("Дмитрий");
        Mockito.when(ioMock.readString()).thenReturn("Левашов");
        Mockito.doNothing().when(ioMock).printOut(Mockito.anyString());

        Mockito.when(messageMock.getMessage(Mockito.anyString(), Mockito.any(), Mockito.any(Locale.class))).thenReturn("Ваша ФИО");

        Student student = console.askStudentInfo();
        assertThat(student)
                .hasFieldOrPropertyWithValue("firstName", "Дмитрий")
                .hasFieldOrPropertyWithValue("surName", "Левашов");
*/
 /*   }

    @Test
    public void askQuestionTest() {
    }

    @Test
    public void printResultTest() {
    }*/
}